{
  "<meta>": {
    "guard": "scripts/atlas-pin-drift.py",
    "purpose": "Pins held back on purpose. A gitlink more than `--max-behind` commits behind its member's default branch is reported by the guard as drift; an entry here explains one instead. A held pin is a debt with an owner, not a steady state.",
    "required": "Every entry needs `member` (the name under repos/), `reason` (why advancing the pin is currently wrong -- name the regression and the filed fix), and `reopen` (the observable event that retires the waiver). An entry missing any of them fails the check, so a waiver cannot decay into an unexplained hold.",
    "scope": "Waivers cover drift only. A pin AHEAD of the member default -- a commit the member does not publish -- is reported as OFF-DEFAULT and is never waivable: it is not a pin held back, it is a pin pointing off the branch.",
    "example": {"member": "example", "reason": "ATLAS-EXAMPLE-2026-09-21: the default tip raises `oversized_files` 4 -> 6, which the conformance ratchet rejects", "reopen": "example PR #12 merges and the count returns to 4"},
    "review": "Re-verify every entry against the member's current default before a sweep advances pins; an entry whose regression the member has since fixed is deleted, not renewed. Measure it the way these were measured: `git -C repos/<member> archive FETCH_HEAD` into a scratch directory, then `python scripts/atlas-conformance.py check --repo <member> --member-path <dir>`, which judges the default tip against the committed baseline without touching the member checkout."
  },
  "waivers": [
    {
      "member": "hephaestus",
      "reason": "ATLAS-GITLINK-DRIFT-056: advancing the pin to master (df40b331d6) raises three ratchet classes against the committed baseline -- oversized_files 37 -> 41, manifest_implementation 14 -> 15, allow_sites 8 -> 9 -- so the advance would fail the conformance gate it is meant to feed. Verified at df40b331d6 on 2026-09-22.",
      "reopen": "hephaestus PR #319 (refactor: Clear the ADR 0061 module-structure ratchet regressions) merges and `atlas-conformance.py check --repo hephaestus` is clean against master; PR #319 was still open when this was written."
    },
    {
      "member": "ritk",
      "reason": "ATLAS-GITLINK-DRIFT-056: advancing the pin to main (e27409344e) raises oversized_files 45 -> 46. The same tip tightens allow_sites 6 -> 0 and type_suffixed_fns 73 -> 71, so this is one file short of an advance that would otherwise lower the baseline. Verified at e27409344e on 2026-09-22.",
      "reopen": "ritk's oversized_files returns to 45 or below on main -- the fix was in progress and no PR was filed when this was written, so the count itself is the trigger, not a PR number."
    }
  ]
}
