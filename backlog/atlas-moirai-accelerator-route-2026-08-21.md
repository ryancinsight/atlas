<a id="atlas-moirai-accelerator-route-2026-08-21"></a>
## ATLAS-MOIRAI-ACCELERATOR-ROUTE-2026-08-21 — Execute accelerator routes [major] [arch] — in-progress

outcome (route-contract slice, delivered): Moirai PR [#147](https://github.com/ryancinsight/Moirai/pull/147) merged at default `ff56d60218b6f418d8db0e42c30da8185b90b6bd`; `RouteResolution` retains `SchedulerRoute` + transport `Address` + accelerator placement. 807/807 nextest, clippy/fmt/doctests/rustdoc green. Dispatch and CPU/WGPU execution remain a later Hephaestus/Themis slice (DAG preserved: Hephaestus consumes Moirai, not the reverse).

open cross-repo correctness/evidence items (independent, dependency-ordered):
- P0 (dispatchable now): kwavers `swe/gpu/solver.rs:92` `propagate_waves_gpu` fakes GPU dispatch (hardcoded timings, ignores inputs) — needs a real kernel + CPU-differential oracle, or withdraw the surface `[major]`. CFDrs `cfd-validation/.../memory.rs:93` ungated `#[global_allocator]` in a lib crate `[major]`. Consus szip unvalidated `u32` reserve (fuzz + typed error needed) and committed `-C target-cpu=native` (remove; use runtime ISA detection) — both owned by Consus PR [#51](https://github.com/ryancinsight/consus/pull/51) `[patch]`.
- P1 (needs #5 first): Hephaestus host/CPU reference implements only 1 of 18 op seams consumers bind (Coeus, Athena, Kwavers) — next bounded slice is SUBSTRATE-003 `[minor]`; unblocks (a) an executed-case counter + software-adapter fallback for GPU suites reporting green having executed nothing, and (b) RITK's unbacked `GpuFieldSmoother`/`CpuOrGpu` speedup claims `[minor]`.
- P2: Themis orphaned test target (14 tests never compiled), CFDrs 54 files/10,543 LOC outside any cargo target, Hermes ADR-005 generator that deletes 14 shipped kernels — each wired-and-green or deleted. Remaining book-fence conversions (Tyche/Proteus/Mnemosyne/Asclepius/Iris, 37 ignored Rust fences) after Gaia's slice closed separately ([gaia#33](https://github.com/ryancinsight/gaia/pull/33)).
- P3: delete the duplicate Leto Krylov impl and migrate callers per Accepted ADR 0033 (Athena is the named owner) `[arch]`; resolve 15 Proposed ADRs (6 kwavers-related) to Accepted/Rejected/deleted; add an MSRV CI job or correct the declared floor for the 9 providers lacking one (Aequitas, Apollo, Harmonia, Helios, Hermes, Horae, Hyperion, Proteus, RITK) `[patch]`.

meta residual: 16/25 submodule checkouts drift from their committed gitlink; kwavers (5), consus (3), helios (3) exceed the two-tree lane bound; 8 empty `worktrees/kwavers-*` orphans — filed, not actioned (every one holds peer state).
