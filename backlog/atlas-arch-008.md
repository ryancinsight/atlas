<a id="atlas-arch-008"></a>
## ATLAS-ARCH-008 — Replace pointer-scattered containers on traversal paths [patch] — in-progress
- Oracle: `scripts/atlas_scattered_containers_classify.py` + `scripts/oracles/arch-008-production-sites.txt`, `make verify-scattered-oracle`. Current: 241 production / 127 test-bench — a pinned census: `--pinned` reads the gitlink commits `HEAD` records straight out of the object store (`ls-tree` + `cat-file --batch`, no checkout, no temp trees), so the gate holds whatever any member has checked out and an oracle can be regenerated for a gitlink sweep without checking 28 members out. Regeneration must ride every pin advance; the count moves when pins move, not when work is staged. Two hardenings found while wiring it: a failed `ls-tree` used to yield an empty file list, so a whole member could leave the census behind a *green* gate (now a loud failure — it hid `repos/metis` under `make`'s different `HOME`), and the member read now scopes `safe.directory` to the one repo named, so `make` and a direct run agree exactly. Six conversions delivered (moirai, ritk-vtk, apollo x2, consus-zarr; CFDrs spectral assembly reverted, 1.2-1.6x slower); measurement narratives live in the delivering PRs.
 - Claims in flight on apollo: PR #529 (stockham `8277871c`) and PR #532 (circle-domain construction linearized, `7eda0710`) — both merged; the Atlas apollo gitlink advances as one unit in this change.
- circle.rs twiddle levels and ritk-filter narrow-band `layers` are correct-as-jagged (verdicts in the apollo/ritk delivery bodies); the ritk `Vec<Vec<usize>>` front-queue defect became `VecDeque` instead.
- Stranded unpushed (prior session's push blocker — unstrand by pushing and opening PRs against each main): gaia `d009178` on `arch-gaia-orient-outward-packed-rows-20260921` (orient_outward flat scaffolding; -76% at 1024, 973 tests pass); ritk `8fe25a9e` on `perf-ritk-filter-narrow-band-deque-20260921` (VecDeque narrow band; -34 to -44%, 1152 pass).
- Residuals: `move_to` relink stays O(len) (pos side-index); the two level-set solvers are near-duplicates (consolidation candidate). Oracle caveat: the 8 ritk-filter sites now typed `Vec<VecDeque<` are invisible to the literal `Vec<Vec<` pattern (widened `--pattern` reads 241; no other member has a `Vec<VecDeque<` site); gaia's +2 re-added sites drop when its branch lands.
- Residuals: `move_to` relink stays O(len) (needs a pos side-index); the two level-set solvers remain near-duplicates (consolidation candidate).
