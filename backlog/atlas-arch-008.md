<a id="atlas-arch-008"></a>
## ATLAS-ARCH-008 — Replace pointer-scattered containers on traversal paths [patch] — in-progress
- Oracle: `scripts/atlas_scattered_containers_classify.py` + `scripts/oracles/arch-008-production-sites.txt`, `make verify-scattered-oracle`. Current: 230 production / 129 test-bench — a pinned census: `--pinned` reads the gitlink commits `HEAD` records straight out of the object store (`ls-tree` + `cat-file --batch`, no checkout, no temp trees), so the gate holds whatever any member has checked out and an oracle can be regenerated for a gitlink sweep without checking 28 members out. Regeneration must ride every pin advance; the count moves when pins move, not when work is staged. Two hardenings found while wiring it: a failed `ls-tree` used to yield an empty file list, so a whole member could leave the census behind a *green* gate (now a loud failure — it hid `repos/metis` under `make`'s different `HOME`), and the member read now scopes `safe.directory` to the one repo named, so `make` and a direct run agree exactly. Eight conversions delivered (moirai, ritk-vtk, apollo x3, consus-zarr, gaia orient_outward; CFDrs spectral assembly reverted, 1.2-1.6x slower); measurement narratives live in the delivering PRs.
 - Claims in flight on apollo: PR #529 (stockham `8277871c`) and PR #532 (circle-domain construction linearized, `7eda0710`) — both merged; Atlas pointer at `0f611c8f` via Atlas #232 (merged).
- circle.rs twiddle levels and ritk-filter narrow-band `layers` are correct-as-jagged (verdicts in the apollo/ritk delivery bodies); the ritk `Vec<Vec<usize>>` front-queue defect became `VecDeque` instead.
- Level-set consolidation delivered 2026-09-22 on ritk branch `refactor/ritk-level-set-evolution-engine`: the "two level-set solvers" residual was actually **four** solvers × `apply`/`apply_native` — eight copies of one curvature→gradient→advection→double-buffer-swap→convergence loop in `ritk-segmentation`. One monomorphized engine (`level_set/helpers/evolve.rs`, generic over zero-sized convergence markers `MaxAbsRate` / `RootMeanSquare`) plus shared `checked_dims`/`binary_mask`/`edge_stopping_fields` replaces them; threshold's minus sign folds into the speed field (`S = −T`) so every increment stays bit-identical to the pre-engine arithmetic. Chan–Vese deliberately stays out: in-place Gauss–Seidel semantics with dirac gating are a different discretisation. Gates green: nextest 485/485, clippy `--all-targets -D warnings`, rustdoc `-D warnings`, fmt; net −140 lines (8 loops → 1 SSOT). Same branch also fixes a manifest defect reproduced on pristine `origin/main`: `ritk-segmentation` never declared the `ritk-core/test-helpers` feature its own rejection tests import, so any `-p ritk-segmentation` test build failed while workspace-wide feature unification (ritk-filter, ritk-registration) hid it — now declared in `[dev-dependencies]` as those members do.
- Residuals: `move_to` relink stays O(len) (pos side-index). Oracle caveat: the 8 ritk-filter sites now typed `Vec<VecDeque<` are invisible to the literal `Vec<Vec<` pattern (widened `--pattern` reads 241; no other member has a `Vec<VecDeque<` site); gaia's +2 sites dropped when the pin swept past the merge.
