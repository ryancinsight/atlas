<a id="next-steps"></a>
## ATLAS-NEXT-STEPS-2026-09-03 - Sequenced plan toward the suite [arch] — todo
outcome: first-party ownership targets stack concerns (scalars, quantities, arrays, allocation, scheduling, placement, accelerators, numerics, physics), never the shell boundary (Tauri/egui/Dioxus stay ecosystem-solved; Metis ADR 0003 tracks them as comparators) — per ADR 0055, refusing `nalgebra`/`ndarray`/`rayon`/`num-traits` inside the stack.
- next: Ares A1-A9 — A0-A8 done, A9 blocked on unpublished `proteus-mat`.
- next: Prometheus P1-P9 — Phase 0 done; **Ask-User** for `ryancinsight/prometheus`.
- not in this plan: no GUI package, no universal model tree, no in-stack UI framework — application layer stays per product behind each tool's IPC.
