<a id="atlas-pin-compile-incoherence-2026-09-22"></a>
## ATLAS-PIN-COMPILE-INCOHERENCE-2026-09-22 — Atlas member pins can be a compile-incoherent set [patch] — todo
- Evidence: twice in two days the overlay build broke at the PIN set, while every member's own CI stayed green — aequitas's default called `scale_by_f64` after eunomia's default renamed it to `scale_by_factor`, until aequitas's follow-up (PR #81) landed. Each member's lock pins its own compatible dependency revs, so member CI proves nothing about the atlas set; the new pins-at-defaults guard (#226) sweeps every gitlink to its default the moment it can, which is exactly the window in which a breaking rename has landed in one member and its consumers' adaptations have not.
- Outcome: an atlas-level instrument measures the pin set's compilability, not just its identity — a bounded CI job checking one representative consumer crate (the rename-prone aequitas/eunomia seam) against the recorded pins, so the drift guard learns "sweep now" from more than per-member green.
- Acceptance oracle: the job fails when any two recorded pins are compile-incompatible (reproduce with the `scale_by_f64` pair by pinning the pre-#81 aequitas commit) and passes at the current coherent set; the drift-guard message names the failing pair.
