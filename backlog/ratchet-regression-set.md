<a id="ratchet-regression-set"></a>
## ATLAS-RATCHET-REGRESSION-SET-2026-09-08 - Seven ratchet regressions arrived with peers' merges [patch] - review
Parent: [`#slop-burndown`](backlog.md#slop-burndown).
outcome: the fleet ratchet returns to zero regressions by fixing the debt, never by raising the baseline. Re-measured 2026-09-23 with `atlas-conformance.py report` on each member's origin default: aequitas `manifest_implementation` 0 (aequitas `260521a`), apollo `existence_only_assertions` 0 (apollo `4ed5ce252`), apollo `manifest_implementation` 24, kwavers `oversized_files` 107, ritk `oversized_files` 43 — all at or under baseline. ritk `type_suffixed_fns` 71 -> 65 in [ritk#620](https://github.com/ryancinsight/ritk/pull/620) (six per-type big-endian readers/writers collapsed into one `BigEndian` trait); this item closes when it lands.
target_forks generator, found 2026-09-23: running cargo from outside the stack with `--manifest-path` and no `CARGO_TARGET_DIR=D:/atlas/target` discovers no atlas config and builds into `<member>/target`; a 1.8 GB `repos/ritk/target` appeared this way within the hour and was deleted. kwavers carries none today. Any instruction to build outside the stack names the target dir in the same line.
