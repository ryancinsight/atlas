docs(atlas): Polish melinoe pointer-advance body-scratch (post code-review eb0abaf)

Apply the 1 minor code-reviewer nit to the melinoe pointer-advance
body-scratch at `/d/atlas/.body-scratch-melinoe-advance`. The
prior code-review (post-`eb0abaf`) flagged that the body-scratch
said "The 10 prior chores in the row 6 chain" but the list
contained 11 SHAs. The fix is a 1-character correction: "10" -> "11".

The 1 str_replace made to `.body-scratch-melinoe-advance`:

  FIX (count correction):
    OLD: "The 10 prior chores in the row 6 chain (`314db47`, `dceed69`, `bcd98ba`, `554e906`, `8179d9f`, `9468382`, `2f18ec4`, `512ff108`, `a21e94b`, `e0bf556`, `b44845a`)"
    NEW: "The 11 prior chores in the row 6 chain (`314db47`, `dceed69`, `bcd98ba`, `554e906`, `8179d9f`, `9468382`, `2f18ec4`, `512ff108`, `a21e94b`, `e0bf556`, `b44845a`)"

The 11 SHAs in the list (in the order cited):
  1. `314db47` (kwavers follow-up advance note)
  2. `dceed69` (kwavers RESOLVED polish)
  3. `bcd98ba` (kwavers bookkeeping regression RESOLVED note)
  4. `554e906` (moirai pointer-advance)
  5. `8179d9f` (moirai closure-mark)
  6. `9468382` (themis closure-mark polish)
  7. `2f18ec4` (row 6 amendment)
  8. `512ff108` (themis closure-mark)
  9. `a21e94b` (themis pointer-advance)
  10. `e0bf556` (e0bf556 row 6 cross-tree reclamation audit)
  11. `b44845a` (KW-CV-001 watchpoint setup)

So the corrected count is 11, not 10.

Per row 10 NO-AMEND rule: this is a NEW atomic chore on top of
the current HEAD (post `35b3edb` CFDrs/eunomia stable/synced).
The `eb0abaf` melinoe pointer-advance commit is NOT amended.
The body-scratch file is a separate artifact from the commit
object — the commit message in `eb0abaf` is permanent and
unchanged. This chore only updates the body-scratch file
itself (which was left in the working tree as an untracked
artifact after the `eb0abaf` chore landed).

Per row 10 forward-fix safety rule: this chore is scoped to
`.body-scratch-melinoe-advance` only. NO submodule gitlink is
modified. NO atlas-meta PM artifact is modified. All 11
submodule pointers are unchanged from the prior `35b3edb`
chore.Disjoint scope: this is a body-scratch metadata polish, not a content change. The melinoe inner WIP, the `eb0abaf` commit message, and the atlas-meta PM artifacts are all preserved verbatim. Only the body-scratch file's count text is corrected. **Permanent record note**: this body-scratch is committed as a permanent record of the count correction because the eb0abaf commit message cannot be amended per row 10 NO-AMEND rule.

Refs:
- `eb0abaf` melinoe pointer-advance (the chore whose
  body-scratch is being polished; the commit message in
  `eb0abaf` is unchanged per the NO-AMEND rule)
- The prior code-review feedback (post-`eb0abaf`) that
  flagged the "10 prior chores" count as inaccurate
- `35b3edb` parent chore (CFDrs/eunomia stable/synced,
  the current HEAD at the time of this polish)
- `6c94595` melinoe closure-mark (post-`eb0abaf` chore
  in the row 6 sequence)

Per row 10 NO-AMEND rule: this is a NEW atomic chore on
top of `35b3edb` (the current HEAD). The `eb0abaf` commit
is not amended.
